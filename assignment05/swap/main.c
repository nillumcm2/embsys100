// This program is used to swap two pointers
#include "Swap.h"

int main()
{
  

  int x = 5;
  int y = 10;
  
  int *ptr_x = &x;
  int *ptr_y = &y;
  
  Swap(&ptr_y, &ptr_x );
  
  
  
  
  
  
  
  //Swap(&ptr_y, &ptr_x );
  
  
  while(1)
  {
    
    
  }
  return 0;
}
